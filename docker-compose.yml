services:
  eldritch-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eldritch-horror-tracker
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
